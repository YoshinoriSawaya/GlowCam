{
    "name": "glow-cam",
    "private": true,
    "version": "0.1.0",
    "scripts": {
        "dev": "npm run dev --prefix frontend",
        "build:wasm": "wasm-pack build gpu_engine --target web",
        "build:fe": "npm run build --prefix frontend"
    }
}